import Vue from 'vue'

import { Button, Cell, Field, CellGroup, NavBar, Toast, CountDown, Icon } from 'vant'

Vue.use(Button)
  .use(Cell)
  .use(CellGroup)
  .use(Field)
  .use(NavBar)
  .use(Toast)
  .use(CountDown)
  .use(Icon)
